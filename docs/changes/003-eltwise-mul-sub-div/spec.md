Implement elementwise binary operations mul, div, sub (multiplication, division, subtraction).
Design and implementation of those operations should be modeled after elemenwise add operation.
Note that those operations should share a lot of code with add operation that already is implemented, in particular kernels should not be duplicaed.
