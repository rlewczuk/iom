**Status:** done

## Summary

SYCL queued copy now consumes detail::copy_metadata_layout / detail::write_copy_metadata / detail::CopyMetadataHeader exclusively; SyclCopyMetadataHeader, SyclMetadataLayout, metadata_layout, write_sycl_metadata, checked_metadata_mul/add, metadata_u64, padded_dimension and the inline plane-decode block deleted from src/sycl/copy.cpp; the parallel_for lambda issues one work item per storage word and calls detail::copy_one_tiled_word. src/shared/standard_tiled_copy.inl gains exactly one new IOM_GPU_DEVICE copy_one_tiled_word (per-word decode + copy_tiled_to_tiled_word) with grid_stride_copy_body looping over it; total_words and the IOM_GPU_GLOBAL_INDEX/STRIDE loop retained; overflow diagnostics and the submission/fence protocol unchanged.

## Verification

- grep audits — deleted-symbol grep (word-boundary) returns zero matches in src/sycl/copy.cpp; shared owners consumed at copy.cpp:585 (copy_metadata_layout), :588 (write_copy_metadata), :601 (CopyMetadataHeader cast), :606 (copy_one_tiled_word); .inl has one copy_one_tiled_word definition (line 466) and its sole call site inside grid_stride_copy_body (line 526); git diff --stat src/cuda src/rocm src/cpu src/ttnn empty and src/shared/standard_tiled_copy.inl shows only the extraction (39+/22-).
- CPU/common: cmake -S . -B build -DBUILD_TESTING=ON -DCUDA_ENABLED=OFF -DROCM_ENABLED=OFF -DSYCL_ENABLED=OFF -DTTNN_ENABLED=OFF && cmake --build build -j --target iom_tests iom_cpu_tests iom_backend_conformance_cpu_tests && ctest --test-dir build --output-on-failure -R "iom_tests|iom_cpu_tests|iom_backend_conformance_cpu_tests" — 3/3 passed (iom_backend_conformance_cpu_tests 30.7s).
- SYCL on Level Zero Arc Pro B60 (bv2): ctest --test-dir build/sycl --output-on-failure -R "^iom_sycl_(smoke|conformance)_tests$|^iom_tests$" — 3/3 passed; focused doctest runs exit 0 for "SYCL queued copy submits one kernel regardless of plane count" (launch count 1 for 2x32x32x64 F32) and for "asynchronous copies against the CPU reference", "storage oracle covers every leaf width and padded shape", "storage oracle identifies perturbed transfer map", "storage and host transfers for every leaf type".
- CUDA (bv1): iom_cuda_conformance_tests full suite — 13/13 cases, 55785 assertions, 0 failed, focused storage/async cases exit 0. ROCm (bv2): iom_rocm_conformance_tests full suite — 14/14 cases, 52624 assertions, 0 failed, focused storage/async cases exit 0. Shared extraction proves behavior-identical kernel expansion.
