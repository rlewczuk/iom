Remarks to current spec:
* please make full review whole spec to catch eventual inconsistencies or issues, make sure to use `@advisor` model extensively to look deeply into the spec and ensure it fits, does not introduce unnecessary complexity, is fairly straightforward and understandable
* pay particular attention to fighting away unnecessary complexity, search for things that can be simplified or got away, ask if unsure;
* always keep in mind that all `DeviceOps.*()` calls are low level operation: their only concern is execution of particular operation on tensors (typically invoking compute kernel for given operation), they never allocate memory (all input, output and scratch buffers need to be provided to them), don't form computation graph, don't provide any other high level functionalities; all those functionalities will be implemented by upper layer which is not present yet and is not concern of this spec;
* make `AddSupport` enum universal across all DeviceOps calls, not only specific to ADD operation: rename it accordingly and promote it to project-wide definition
